"""Bundled project seeds.

Each stock project is a JSON file under `seeds/projects/`. They are the
only source of project content from Phase 1 onwards — the in-Python
`PROJECT_TEMPLATES` dict is gone.
"""
