ultralytics>=8.3.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
requests>=2.32.0
# Linear-assignment solver for ByteTrack (model.track). Without it the
# sidecar falls back to plain detection.
lapx>=0.5.11
